import React from 'react';
import Sidebar from '../components/RightSidebar';

const Following = () => {
  return (
    <div>
      {/* Reuse the Policies component */}
      <Sidebar />
    </div>
  );
};

export default Following;